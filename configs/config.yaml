defaults:
  - data: default
  - model: distilbert
  - train: default
  - logger: default
  - export: default
  - baseline: default
  - _self_

hydra:
  run:
    dir: ./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
